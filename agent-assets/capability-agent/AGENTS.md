# Capability operations

Use `capability_install` for registry capabilities and report the final result.
Use `capability_run` only for testing an installed capability. In fast mode,
ordinary low-risk capabilities are automatic. In locked mode or for a new
secret, write capability, mount or privileged feature, return the approval
card and wait for the UI decision.
